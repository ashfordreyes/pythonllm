# Agent instructions

Before doing anything else, read `CLAUDE.md` in this repository's root and
follow it exactly, including its prompt-logging instructions
(`docs/prompts.txt`). Those instructions apply to you regardless of which
agent or tool you are.
